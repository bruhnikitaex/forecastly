# 🧠 Forecastly — система анализа и прогнозирования продаж

> Дипломный проект студента **Вульферт Никита Евгеньевич**, группа **122 ИСП**  
> Новосибирский политехнический колледж, 2025 год

---

## 📘 О проекте

**Forecastly** — это система, предназначенная для анализа и прогнозирования продаж компании.  
Решение объединяет в себе **ETL-обработку данных**, **машинное обучение (Prophet и LightGBM)**,  
**интерактивный дашборд на Streamlit** и **REST API на FastAPI**.

### ✨ Основные возможности:
- 📊 Загрузка, очистка и валидация данных о продажах
- 🔍 Анализ динамики спроса и ключевых показателей
- 🤖 Обучение и сравнение моделей прогнозирования (Prophet, LightGBM, Ensemble)
- 📈 Визуализация прогнозов с интерактивными графиками
- 🧩 Экспорт метрик и истории прогнозов
- 🌐 REST API для интеграции с внешними системами (Power BI, Excel, 1C)
- 🐳 **Docker контейнеризация** для простого развёртывания
- ⚡ Оптимизированное кэширование данных в Streamlit

---

## 🏗️ Архитектура проекта

```
forecastly/
├── src/
│   ├── api/                    # FastAPI REST-сервис
│   │   ├── app.py             # Основной API с versioning (/api/v1/...)
│   │   └── schemas.py         # Pydantic схемы
│   ├── etl/                    # ETL-конвейер
│   │   ├── load_data.py       # Загрузка CSV
│   │   ├── clean_data.py      # Очистка и нормализация
│   │   ├── validate.py        # Валидация входных данных ✨
│   │   ├── feature_builder.py # Построение признаков
│   │   └── ...
│   ├── models/                 # ML модели
│   │   ├── train_lgbm.py      # Обучение LightGBM
│   │   ├── train_prophet.py   # Обучение Prophet
│   │   ├── predict.py         # Прогнозирование
│   │   └── evaluate.py        # Оценка качества
│   ├── ui/                     # Streamlit дашборд
│   │   └── dashboard.py       # Интерактивный интерфейс с кэшированием ✨
│   └── utils/                  # Утилиты
│       ├── config.py          # Конфиг с поддержкой .env ✨
│       ├── logger.py          # Логирование
│       └── helpers.py         # Вспомогательные функции
├── data/
│   ├── raw/                    # Сырые данные
│   ├── processed/              # Обработанные данные
│   └── models/                 # Сохранённые модели
├── configs/
│   ├── paths.yaml             # Пути к файлам
│   └── model.yaml             # Параметры моделей
├── docs/
│   └── api.md                 # Документация API
├── Dockerfile                 # Docker образ API ✨
├── Dockerfile.streamlit       # Docker образ дашборда ✨
├── docker-compose.yml         # Docker Compose (API + Dashboard + DB) ✨
├── .env.example               # Пример конфигурации .env ✨
├── .gitignore                 # Git ignore rules
├── requirements.txt           # Python зависимости с версионированием ✨
└── README.md
```

**✨ = Новые/улучшенные компоненты**

---

## 🚀 Запуск проекта

### Вариант 1: Локальный запуск (без Docker)

#### 1. Установка зависимостей
```bash
# Клонирование
git clone https://github.com/bruhnikita/forecastly.git
cd forecastly

# Создание виртуального окружения
python -m venv venv
venv\Scripts\activate     # Windows
source venv/bin/activate  # Linux/Mac

# Установка зависимостей
pip install -r requirements.txt

# Копирование .env файла (опционально)
cp .env.example .env
```

#### 2. Запуск Streamlit дашборда
```bash
streamlit run src/ui/dashboard.py
```
Откроется: **http://localhost:8501**

#### 3. Запуск API сервера (в отдельном терминале)
```bash
uvicorn src.api.app:app --reload --port 8000
```
Документация: **http://localhost:8000/docs**

---

### Вариант 2: Запуск через Docker Compose (рекомендуется для production)

```bash
# Запуск всех сервисов (API + Dashboard + PostgreSQL)
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f api
docker-compose logs -f dashboard

# Остановка
docker-compose down
```

**Доступные сервисы:**
- **API**: http://localhost:8000 (документация: /docs)
- **Dashboard**: http://localhost:8501
- **PostgreSQL**: localhost:5432 (для будущей интеграции)

---

## 🌐 REST API

Все endpoints версионированы с префиксом `/api/v1/`.

### Основные endpoints:

```bash
# Список доступных SKU
GET /api/v1/skus

# Получить прогноз по SKU
GET /api/v1/predict?sku_id=SKU001&horizon=14

# Пересчитать прогнозы
POST /api/v1/predict/rebuild?horizon=14

# Получить метрики качества
GET /api/v1/metrics

# Статус системы
GET /api/v1/status

# Health check
GET /health
```

### Пример использования:
```python
import requests

response = requests.get('http://localhost:8000/api/v1/skus')
print(response.json())

forecast = requests.get('http://localhost:8000/api/v1/predict', 
                       params={'sku_id': 'SKU001', 'horizon': 14})
print(forecast.json())
```

Полная документация: [docs/api.md](docs/api.md)

---

## 🎯 Ключевые улучшения в версии 2.0

✅ **Валидация данных** — комплексная проверка входных CSV файлов  
✅ **Обработка ошибок** — try-catch блоки и детальное логирование  
✅ **Docstrings** — полная документация всех функций (Google style)  
✅ **Environment переменные** — поддержка `.env` для конфигурации  
✅ **API versioning** — все endpoints под `/api/v1/`  
✅ **Docker** — Dockerfile + docker-compose для контейнеризации  
✅ **Кэширование** — @st.cache_data в Streamlit для оптимизации  
✅ **Requirements** — версионирование всех зависимостей  

---

## 📊 Примеры использования
git clone https://github.com/bruhnikita/forecastly.git
cd forecastly
2. Создание виртуального окружения
bash
Копировать код
python -m venv venv
venv\Scripts\activate     # для Windows
source venv/bin/activate  # для Linux/Mac
3. Установка зависимостей
bash
Копировать код
pip install -r requirements.txt
🚀 Запуск системы
📊 Запуск дашборда Streamlit
bash
Копировать код
streamlit run src/ui/dashboard.py
Открой в браузере: http://localhost:8501

🌐 Запуск API-сервера
bash
Копировать код
uvicorn src.api.app:app --reload --port 8000
Открой в браузере:

Swagger UI → http://127.0.0.1:8000/docs

ReDoc → http://127.0.0.1:8000/redoc

Документация по API доступна также в файле docs/api.md.

🧩 Основные компоненты
Компонент	Назначение
src/etl	Сбор и очистка данных (ETL-конвейер)
src/models	Обучение моделей Prophet и LightGBM, прогнозирование
src/ui/dashboard.py	Веб-интерфейс на Streamlit
src/api/app.py	REST API для интеграции
src/utils/logger.py	Логирование событий
data/processed/history/	История прогнозов (автоматически сохраняется)

📈 Пример работы
Пример прогноза в дашборде:

Дата	Факт	Prophet	LGBM	Ensemble
2025-10-30	21	22.3	20.8	21.6
2025-10-31	24	23.1	22.0	22.6
2025-11-01	23	24.4	22.8	23.6

📚 Используемые технологии
Область	Инструменты
Язык программирования	Python 3.11
Веб-интерфейс	Streamlit
API	FastAPI + Uvicorn
Машинное обучение	Prophet, LightGBM, scikit-learn
Аналитика данных	Pandas, NumPy, Matplotlib, Seaborn
Логирование	Loguru
Прочее	Git, Virtualenv

📜 Документация
API (docs/api.md)

Модуль ETL

Обучение моделей

Панель визуализации

🧠 Автор
Вульферт Никита Евгеньевич
Группа 122 ИСП, Новосибирский политехнический колледж
Руководитель проекта: Гритчин И.В.

🏁 Лицензия
Проект выполнен в образовательных целях.
Все материалы можно использовать для учебных и исследовательских проектов.